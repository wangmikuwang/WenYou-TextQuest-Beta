# 项目接管审核 · 2026-09-20

基于 `bef85a6` 审核。检查了启动与预设导入、JSON 资料库、分支引擎、对局状态机、AI 请求/解析、分享码和编辑页面；本轮修复版本为 v1.9.1，未创建 GitHub Release。

## 本轮修复

| 优先级 | 问题与触发方式 | 修复 |
| --- | --- | --- |
| P1 | 资料库的多个 IO 协程同时读取旧列表并覆盖写回；修复前 80 次并发新增只留下 2 条 | `LocalLibrary` 统一锁保护读改写和导出快照 |
| P1 | 直接覆盖 JSON，写盘异常被吞掉，调用方仍显示保存成功 | 临时文件写入、同步后原子替换；单文件成功后更新内存，失败统一提示并中止成功分支 |
| P1 | 选择日志刚追加就被旧 session 覆盖，跳转还会丢失掷骰说明；自循环状态面板不更新 | 使用最新 session 执行效果和跳转；统一同步 UI 会话 |
| P1 | AI 场景解码后重新构造对象，丢掉 `state`，角色面板因此不变化 | 保留已解码状态变化和其他字段，仅替换清洗后的正文与选项 |
| P1 | OpenAI 兼容响应同时含正文和思考时，只取思考，正文丢失 | 流式和非流式路径分别保留两类文本及对应回调 |
| P1 | 分享码解压没有输出上限，且可接受压缩流未正常结束的负载 | 限制输入及解压大小，要求压缩流完整结束，始终释放 Inflater；超大导出明确提示 |
| P2 | 读档重复追加当前叙述/结局；无文本开场的 session 可能不发布到 UI | 读档只恢复页面状态，新增正文仅在实际进入节点时记录 |
| P2 | 无选项节点互相自动跳转，导致递归循环 | 检测同一自动跳转链中的重复节点并显示剧情错误 |
| P2 | 条件指定不存在的角色时，错误回退到同名全局变量和标记 | 使用空角色状态求值，不混用全局状态 |
| P2 | 骰子面数到达 Int.MAX_VALUE 时，`faces + 1` 溢出导致崩溃 | 使用 `nextInt(faces) + 1`，全局和角色效果均修复 |
| P2 | 正文清洗遇到“【正文】”等标题时保留了前面的思考，丢掉后面的正文 | 保留正文标题之后的内容，保留正文分隔线两侧的叙述 |
| P2 | Lint 有 3 个阻断错误 | 声明 CameraX 实验 API、以 Compose 状态订阅读取角色列表、声明相机硬件可选 |
| P2 | AI 动态选项只存在于页面状态，读取 AI 节点存档会再次请求模型 | 动态选项和“已生成待选择”状态写入 `SessionState`，读档直接恢复 |
| P2 | 构建输出目录硬编码为单个 Windows 用户路径 | 默认改到 Gradle 用户缓存，可用 `WENYOU_BUILD_DIR` 覆盖 |
| P2 | 两套页数相同的 QR Book 可在连续扫描时被混拼 | 新分片加入整包 CRC 标识，扫描切换整包时清空旧页；继续兼容旧格式 |

## 验证

- α/β 各 11 项 JVM 回归测试，均通过。测试文件：`app/src/test/java/io/wenyou/textquest/RegressionTest.kt`。
- `lintBetaDebug`、`assembleBetaDebug` 均通过，没有通过 baseline 或禁用规则绕过错误。
- 写盘测试包含 80 次并发新增、重新加载一致性、序列化失败及目标路径无法替换时保护已有数据。
- AI 测试使用 OkHttp 拦截器模拟流式和非流式响应，不调用外部模型。
- Pixel 7 / Android 34 模拟器：最终 α/β Debug APK 均安装并冷启动成功，崩溃缓冲区没有新增记录。α 版完成“雨夜咖啡馆 → 帮忙打烊 → 掷骰 → 存档 → 重启应用 → 主页续玩 → 再存档”，两次存档均为 4 条记录，选择、掷骰和正文未丢失或重复。

复现命令：

```powershell
.\gradlew.bat :app:testBetaDebugUnitTest :app:lintBetaDebug :app:assembleBetaDebug
```

## 仍需后续处理或实机验证

- **跨文件事务（P2）**：整包导入和关联删除涉及多个 JSON 文件，当前仅保证单文件原子替换。中途写盘失败可能留下部分更新；本轮保留现有文件格式，没有引入数据库或事务日志。
- 相机扫码、相册导入、真实第三方 AI 服务及断网/切后台行为尚未完成设备集成验证。模拟响应通过不代表所有服务协议均已验证。
- 仍有非阻断的依赖版本、弃用 API、资源和图标警告；当前 Android Gradle Plugin 与部分 Navigation Lint 检查器版本不匹配，部分库检查被工具跳过。本轮没有顺带升级依赖。
